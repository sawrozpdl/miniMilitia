class Camera {

    constructor(game) {
        this.game = game;
        this.player = this.game.player;
    }

    update () {
        
    }

    setScope(scope) { // 2x, 4x, 6x (max)

    }
}

export default Camera;